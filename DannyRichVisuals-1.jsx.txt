import { useState, useEffect, useRef } from "react";

// Logo SVG embedded directly from the uploaded image description
const LogoSVG = ({ size = 40, white = true }) => (
  <svg width={size * 2.8} height={size * 0.85} viewBox="0 0 280 85" fill="none" xmlns="http://www.w3.org/2000/svg">
    {/* Star/burst icon */}
    <g transform="translate(42,42)">
      {[0,30,60,90,120,150,180,210,240,270,300,330].map((angle, i) => (
        <rect key={i} x="-3" y="-38" width="6" height="18" rx="2"
          fill={white ? "white" : "#a855f7"}
          transform={`rotate(${angle})`} opacity={i % 2 === 0 ? 1 : 0.7} />
      ))}
      <rect x="-9" y="-9" width="18" height="18" rx="3" fill="black" />
      <circle cx="0" cy="0" r="5" fill={white ? "white" : "#a855f7"} />
    </g>
    {/* Text */}
    <text x="95" y="35" fontFamily="'Arial Black', Arial" fontWeight="900" fontSize="22" fill={white ? "white" : "#a855f7"} letterSpacing="2">DANNY RICH</text>
    <text x="95" y="62" fontFamily="'Arial Black', Arial" fontWeight="900" fontSize="22" fill={white ? "white" : "#a855f7"} letterSpacing="2">VISUALS</text>
    <text x="260" y="62" fontFamily="serif" fontSize="12" fill={white ? "white" : "#a855f7"}>®</text>
  </svg>
);

const NAV_LINKS = ["Home", "About", "Services", "Portfolio", "Booking", "Contact"];

const SERVICES = [
  { icon: "✦", title: "Flyer Design", desc: "Eye-catching flyers that command attention and communicate your message with power.", price: "From $15" },
  { icon: "◈", title: "Social Media Design", desc: "Premium social media graphics that elevate your brand presence across all platforms.", price: "From $20" },
  { icon: "◉", title: "Branding", desc: "Complete brand identity systems that make your business unforgettable.", price: "From $80" },
  { icon: "◆", title: "Event Promotion", desc: "Stunning event visuals that build hype and drive attendance.", price: "From $25" },
  { icon: "✸", title: "Church Media", desc: "Inspiring church flyers and media designs that spread your message.", price: "From $15" },
  { icon: "⬡", title: "Logo Design", desc: "Distinctive logos crafted with precision to represent your brand identity.", price: "From $50" },
  { icon: "◇", title: "Content Design", desc: "Creative content visuals for digital campaigns and marketing materials.", price: "From $20" },
];

const TESTIMONIALS = [
  { name: "Marcus A.", role: "Event Organizer", text: "Danny Rich Visuals transformed my event flyers into something truly cinematic. My audience was stunned before they even arrived!" },
  { name: "Pastor Grace O.", role: "Church Leader", text: "The church media designs are breathtaking. Our congregation loves the professionalism and spiritual depth in every piece." },
  { name: "Tunde B.", role: "Business Owner", text: "My brand identity completely changed. Clients now see me as premium. Worth every penny and more." },
  { name: "Chiamaka N.", role: "Content Creator", text: "Fastest turnaround and highest quality I've ever experienced. Danny Rich Visuals is my go-to designer forever." },
];

const PORTFOLIO_ITEMS = [
  { cat: "Branding", label: "TechVision Brand", color: "#7c3aed", h: 280 },
  { cat: "Flyer", label: "Night Event Flyer", color: "#4c1d95", h: 220 },
  { cat: "Social", label: "Instagram Campaign", color: "#5b21b6", h: 260 },
  { cat: "Logo", label: "Luxe Logo Design", color: "#6d28d9", h: 200 },
  { cat: "Church", label: "Sunday Service Media", color: "#4c1d95", h: 240 },
  { cat: "Event", label: "Music Fest Promo", color: "#7c3aed", h: 290 },
  { cat: "Social", label: "Brand Story Posts", color: "#5b21b6", h: 210 },
  { cat: "Branding", label: "Startup Identity", color: "#6d28d9", h: 250 },
  { cat: "Flyer", label: "Business Grand Opening", color: "#4c1d95", h: 230 },
];

const STATS = [
  { val: 100, label: "Designs Completed", suffix: "+" },
  { val: 50, label: "Happy Clients", suffix: "+" },
  { val: 3, label: "Years Experience", suffix: "+" },
  { val: 24, label: "Creative Support", suffix: "/7" },
];

const PROCESS = [
  { num: "01", title: "Understanding the Vision", desc: "Deep dive into your brand, goals, and creative direction." },
  { num: "02", title: "Research & Concept", desc: "Market research and concept development tailored to your audience." },
  { num: "03", title: "Design Execution", desc: "Crafting premium visuals with precision and creative excellence." },
  { num: "04", title: "Review & Refinement", desc: "Collaborative feedback rounds to perfect every detail." },
  { num: "05", title: "Final Delivery", desc: "High-quality files delivered in all formats you need." },
];

const FAQ = [
  { q: "How long does a design take?", a: "Most designs are completed within 24–72 hours. Complex branding projects may take 5–7 days." },
  { q: "Do you offer revisions?", a: "Yes! Every order includes revision rounds to ensure you're 100% satisfied." },
  { q: "What formats will I receive?", a: "You'll receive print-ready and digital files: PNG, JPG, PDF, and editable source files upon request." },
  { q: "How do I place an order?", a: "Use the Booking page, WhatsApp us directly, or send an email with your project details." },
  { q: "Can I see samples before paying?", a: "Visit our Portfolio page to explore our full range of work across all design categories." },
];

// Animated counter hook
function useCounter(target, active) {
  const [count, setCount] = useState(0);
  useEffect(() => {
    if (!active) return;
    let start = 0;
    const step = Math.ceil(target / 60);
    const timer = setInterval(() => {
      start += step;
      if (start >= target) { setCount(target); clearInterval(timer); }
      else setCount(start);
    }, 25);
    return () => clearInterval(timer);
  }, [active, target]);
  return count;
}

function StatCard({ val, label, suffix, active }) {
  const count = useCounter(val, active);
  return (
    <div style={{ textAlign: "center", padding: "2rem" }}>
      <div style={{ fontSize: "3.5rem", fontWeight: 900, background: "linear-gradient(135deg, #a855f7, #7c3aed)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent", fontFamily: "'Arial Black', Arial" }}>
        {count}{suffix}
      </div>
      <div style={{ color: "#d4b8ff", fontSize: "0.95rem", marginTop: "0.5rem", letterSpacing: "0.05em" }}>{label}</div>
    </div>
  );
}

// Scroll animation hook
function useInView(ref) {
  const [inView, setInView] = useState(false);
  useEffect(() => {
    const obs = new IntersectionObserver(([e]) => { if (e.isIntersecting) setInView(true); }, { threshold: 0.2 });
    if (ref.current) obs.observe(ref.current);
    return () => obs.disconnect();
  }, []);
  return inView;
}

function FadeIn({ children, delay = 0, style = {} }) {
  const ref = useRef();
  const inView = useInView(ref);
  return (
    <div ref={ref} style={{
      opacity: inView ? 1 : 0,
      transform: inView ? "translateY(0)" : "translateY(40px)",
      transition: `opacity 0.7s ease ${delay}s, transform 0.7s ease ${delay}s`,
      ...style
    }}>{children}</div>
  );
}

export default function DannyRichVisuals() {
  const [page, setPage] = useState("Home");
  const [menuOpen, setMenuOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);
  const [portfolioFilter, setPortfolioFilter] = useState("All");
  const [modalItem, setModalItem] = useState(null);
  const [bookingStep, setBookingStep] = useState(1);
  const [bookingData, setBookingData] = useState({ service: "", budget: "", deadline: "", name: "", email: "", phone: "", details: "" });
  const [faqOpen, setFaqOpen] = useState(null);
  const [toastMsg, setToastMsg] = useState("");
  const statsRef = useRef();
  const statsInView = useInView(statsRef);
  const [heroAnim, setHeroAnim] = useState(false);
  const [testimonialIdx, setTestimonialIdx] = useState(0);

  useEffect(() => { setTimeout(() => setHeroAnim(true), 100); }, []);
  useEffect(() => {
    const h = () => setScrolled(window.scrollY > 40);
    window.addEventListener("scroll", h);
    return () => window.removeEventListener("scroll", h);
  }, []);
  useEffect(() => {
    const t = setInterval(() => setTestimonialIdx(i => (i + 1) % TESTIMONIALS.length), 4000);
    return () => clearInterval(t);
  }, []);

  const showToast = (msg) => { setToastMsg(msg); setTimeout(() => setToastMsg(""), 3000); };

  const navigate = (p) => { setPage(p); setMenuOpen(false); window.scrollTo(0, 0); };

  const cats = ["All", ...Array.from(new Set(PORTFOLIO_ITEMS.map(p => p.cat)))];
  const filteredPortfolio = portfolioFilter === "All" ? PORTFOLIO_ITEMS : PORTFOLIO_ITEMS.filter(p => p.cat === portfolioFilter);

  // Styles
  const S = {
    root: { fontFamily: "'Trebuchet MS', Tahoma, sans-serif", background: "#050508", color: "white", minHeight: "100vh", overflowX: "hidden" },
    nav: {
      position: "fixed", top: 0, left: 0, right: 0, zIndex: 1000,
      background: scrolled ? "rgba(5,5,8,0.95)" : "transparent",
      backdropFilter: scrolled ? "blur(20px)" : "none",
      borderBottom: scrolled ? "1px solid rgba(168,85,247,0.15)" : "none",
      transition: "all 0.4s ease", padding: "0 2rem", height: "70px",
      display: "flex", alignItems: "center", justifyContent: "space-between"
    },
    navLink: (active) => ({
      background: "none", border: "none", color: active ? "#a855f7" : "rgba(255,255,255,0.75)",
      cursor: "pointer", fontSize: "0.9rem", fontWeight: active ? 700 : 500,
      letterSpacing: "0.08em", textTransform: "uppercase", padding: "0.5rem 0.75rem",
      transition: "color 0.3s", fontFamily: "inherit",
      borderBottom: active ? "2px solid #a855f7" : "2px solid transparent",
    }),
    glowBtn: (small = false) => ({
      background: "linear-gradient(135deg, #7c3aed, #a855f7)",
      border: "none", borderRadius: "50px", color: "white",
      padding: small ? "0.6rem 1.5rem" : "0.9rem 2.2rem",
      fontSize: small ? "0.85rem" : "1rem", fontWeight: 700,
      cursor: "pointer", letterSpacing: "0.05em",
      boxShadow: "0 0 25px rgba(168,85,247,0.5)",
      transition: "all 0.3s ease", fontFamily: "inherit",
    }),
    outlineBtn: (small = false) => ({
      background: "transparent", border: "2px solid #a855f7",
      borderRadius: "50px", color: "#a855f7",
      padding: small ? "0.55rem 1.4rem" : "0.85rem 2.1rem",
      fontSize: small ? "0.85rem" : "1rem", fontWeight: 700,
      cursor: "pointer", letterSpacing: "0.05em",
      transition: "all 0.3s ease", fontFamily: "inherit",
    }),
    glass: {
      background: "rgba(168,85,247,0.05)", backdropFilter: "blur(10px)",
      border: "1px solid rgba(168,85,247,0.2)", borderRadius: "20px",
    },
    section: { padding: "6rem 2rem", maxWidth: "1200px", margin: "0 auto" },
    heading: { fontSize: "clamp(2rem, 5vw, 3.5rem)", fontWeight: 900, lineHeight: 1.1, fontFamily: "'Arial Black', Arial" },
    subheading: { color: "#c4b5fd", fontSize: "1.1rem", lineHeight: 1.7, maxWidth: "600px" },
    tag: { display: "inline-block", background: "rgba(168,85,247,0.15)", border: "1px solid rgba(168,85,247,0.3)", borderRadius: "50px", padding: "0.3rem 1rem", fontSize: "0.8rem", color: "#c4b5fd", letterSpacing: "0.1em", textTransform: "uppercase", marginBottom: "1.5rem" },
    input: { background: "rgba(168,85,247,0.07)", border: "1px solid rgba(168,85,247,0.3)", borderRadius: "12px", color: "white", padding: "0.9rem 1.2rem", fontSize: "1rem", width: "100%", outline: "none", fontFamily: "inherit", transition: "border 0.3s" },
    select: { background: "rgba(10,5,20,0.9)", border: "1px solid rgba(168,85,247,0.3)", borderRadius: "12px", color: "white", padding: "0.9rem 1.2rem", fontSize: "1rem", width: "100%", outline: "none", fontFamily: "inherit" },
  };

  // ─── PAGES ───────────────────────────────────────────────────────────────────

  const HomePage = () => (
    <div>
      {/* Hero */}
      <section style={{ minHeight: "100vh", display: "flex", alignItems: "center", position: "relative", overflow: "hidden" }}>
        {/* Bg effects */}
        <div style={{ position: "absolute", inset: 0, background: "radial-gradient(ellipse at 60% 40%, rgba(124,58,237,0.25) 0%, transparent 60%), radial-gradient(ellipse at 20% 80%, rgba(88,28,221,0.15) 0%, transparent 50%)" }} />
        <div style={{ position: "absolute", top: "10%", right: "5%", width: "500px", height: "500px", borderRadius: "50%", background: "radial-gradient(circle, rgba(168,85,247,0.12) 0%, transparent 70%)", animation: "pulse 4s ease-in-out infinite" }} />
        {/* Floating shapes */}
        {[...Array(6)].map((_, i) => (
          <div key={i} style={{
            position: "absolute",
            width: `${20 + i * 15}px`, height: `${20 + i * 15}px`,
            border: `1px solid rgba(168,85,247,${0.1 + i * 0.05})`,
            borderRadius: i % 2 === 0 ? "50%" : "4px",
            left: `${10 + i * 15}%`, top: `${20 + i * 10}%`,
            animation: `float${i % 3} ${3 + i}s ease-in-out infinite`,
            transform: `rotate(${i * 30}deg)`
          }} />
        ))}
        <div style={{ ...S.section, display: "flex", flexDirection: "column", gap: "2rem", paddingTop: "10rem" }}>
          <div style={{ ...S.tag, opacity: heroAnim ? 1 : 0, transition: "opacity 0.8s 0.2s" }}>Premium Creative Studio</div>
          <h1 style={{
            ...S.heading, fontSize: "clamp(2.5rem, 6vw, 5rem)",
            opacity: heroAnim ? 1 : 0, transform: heroAnim ? "translateY(0)" : "translateY(30px)",
            transition: "all 0.8s 0.3s ease",
            maxWidth: "800px"
          }}>
            Turning Creative Ideas Into{" "}
            <span style={{ background: "linear-gradient(135deg, #a855f7, #7c3aed, #c084fc)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>
              Stunning Visual Experiences
            </span>
          </h1>
          <p style={{
            ...S.subheading,
            opacity: heroAnim ? 1 : 0, transform: heroAnim ? "translateY(0)" : "translateY(20px)",
            transition: "all 0.8s 0.5s ease",
          }}>
            Premium graphic designs that elevate brands, businesses, churches, and events through creativity and visual excellence.
          </p>
          <div style={{ display: "flex", gap: "1rem", flexWrap: "wrap", opacity: heroAnim ? 1 : 0, transition: "opacity 0.8s 0.7s" }}>
            <button style={S.glowBtn()} onClick={() => navigate("Portfolio")} onMouseEnter={e => { e.target.style.transform = "translateY(-2px)"; e.target.style.boxShadow = "0 0 40px rgba(168,85,247,0.8)"; }} onMouseLeave={e => { e.target.style.transform = ""; e.target.style.boxShadow = "0 0 25px rgba(168,85,247,0.5)"; }}>
              View Portfolio
            </button>
            <button style={S.glowBtn()} onClick={() => navigate("Booking")} onMouseEnter={e => { e.target.style.transform = "translateY(-2px)"; e.target.style.boxShadow = "0 0 40px rgba(168,85,247,0.8)"; }} onMouseLeave={e => { e.target.style.transform = ""; e.target.style.boxShadow = "0 0 25px rgba(168,85,247,0.5)"; }}>
              Book a Design
            </button>
            <button style={S.outlineBtn()} onClick={() => navigate("Contact")} onMouseEnter={e => { e.target.style.background = "rgba(168,85,247,0.1)"; }} onMouseLeave={e => { e.target.style.background = "transparent"; }}>
              Contact Us
            </button>
          </div>
        </div>
        {/* Scroll indicator */}
        <div style={{ position: "absolute", bottom: "2rem", left: "50%", transform: "translateX(-50%)", animation: "bounce 2s infinite", display: "flex", flexDirection: "column", alignItems: "center", gap: "0.5rem" }}>
          <span style={{ color: "rgba(168,85,247,0.6)", fontSize: "0.75rem", letterSpacing: "0.2em" }}>SCROLL</span>
          <div style={{ width: "1px", height: "40px", background: "linear-gradient(to bottom, #a855f7, transparent)" }} />
        </div>
      </section>

      {/* Stats */}
      <section ref={statsRef} style={{ background: "rgba(124,58,237,0.05)", borderTop: "1px solid rgba(168,85,247,0.1)", borderBottom: "1px solid rgba(168,85,247,0.1)", padding: "3rem 2rem" }}>
        <div style={{ maxWidth: "1200px", margin: "0 auto", display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(180px, 1fr))", gap: "1rem" }}>
          {STATS.map((s, i) => <StatCard key={i} {...s} active={statsInView} />)}
        </div>
      </section>

      {/* Services Overview */}
      <section style={{ padding: "6rem 2rem" }}>
        <div style={{ maxWidth: "1200px", margin: "0 auto" }}>
          <FadeIn><div style={S.tag}>What We Do</div></FadeIn>
          <FadeIn delay={0.1}><h2 style={{ ...S.heading, marginBottom: "1rem" }}>Creative Services<br /><span style={{ background: "linear-gradient(135deg,#a855f7,#7c3aed)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>Built to Elevate</span></h2></FadeIn>
          <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(280px, 1fr))", gap: "1.5rem", marginTop: "3rem" }}>
            {SERVICES.slice(0, 6).map((sv, i) => (
              <FadeIn key={i} delay={i * 0.08}>
                <div style={{ ...S.glass, padding: "2rem", height: "100%", cursor: "pointer", transition: "all 0.3s" }}
                  onMouseEnter={e => { e.currentTarget.style.borderColor = "rgba(168,85,247,0.6)"; e.currentTarget.style.boxShadow = "0 0 30px rgba(168,85,247,0.15)"; e.currentTarget.style.transform = "translateY(-5px)"; }}
                  onMouseLeave={e => { e.currentTarget.style.borderColor = "rgba(168,85,247,0.2)"; e.currentTarget.style.boxShadow = "none"; e.currentTarget.style.transform = ""; }}>
                  <div style={{ fontSize: "2rem", marginBottom: "1rem", color: "#a855f7" }}>{sv.icon}</div>
                  <h3 style={{ fontWeight: 800, marginBottom: "0.5rem", fontSize: "1.1rem" }}>{sv.title}</h3>
                  <p style={{ color: "#9ca3af", fontSize: "0.9rem", lineHeight: 1.6 }}>{sv.desc}</p>
                  <div style={{ marginTop: "1.5rem", color: "#c4b5fd", fontWeight: 700, fontSize: "0.9rem" }}>{sv.price}</div>
                </div>
              </FadeIn>
            ))}
          </div>
          <div style={{ textAlign: "center", marginTop: "3rem" }}>
            <button style={S.outlineBtn()} onClick={() => navigate("Services")} onMouseEnter={e => e.target.style.background = "rgba(168,85,247,0.1)"} onMouseLeave={e => e.target.style.background = "transparent"}>
              View All Services →
            </button>
          </div>
        </div>
      </section>

      {/* Featured Portfolio */}
      <section style={{ padding: "6rem 2rem", background: "rgba(124,58,237,0.04)" }}>
        <div style={{ maxWidth: "1200px", margin: "0 auto" }}>
          <FadeIn><div style={S.tag}>Portfolio</div></FadeIn>
          <FadeIn delay={0.1}><h2 style={{ ...S.heading, marginBottom: "3rem" }}>Featured <span style={{ background: "linear-gradient(135deg,#a855f7,#7c3aed)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>Works</span></h2></FadeIn>
          <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(260px, 1fr))", gap: "1.5rem" }}>
            {PORTFOLIO_ITEMS.slice(0, 6).map((item, i) => (
              <FadeIn key={i} delay={i * 0.07}>
                <div style={{ borderRadius: "16px", overflow: "hidden", cursor: "pointer", position: "relative", height: `${item.h}px`, background: `linear-gradient(135deg, ${item.color}, #1a0533)`, transition: "transform 0.3s" }}
                  onMouseEnter={e => { e.currentTarget.style.transform = "scale(1.03)"; e.currentTarget.querySelector(".overlay").style.opacity = "1"; }}
                  onMouseLeave={e => { e.currentTarget.style.transform = ""; e.currentTarget.querySelector(".overlay").style.opacity = "0"; }}
                  onClick={() => setModalItem(item)}>
                  {/* Decorative design preview */}
                  <div style={{ position: "absolute", inset: 0, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: "1rem" }}>
                    <div style={{ width: "60px", height: "60px", border: "2px solid rgba(255,255,255,0.3)", borderRadius: "50%", display: "flex", alignItems: "center", justifyContent: "center", fontSize: "1.5rem" }}>✦</div>
                    <div style={{ width: "80%", height: "2px", background: "rgba(255,255,255,0.1)" }} />
                    <div style={{ width: "60%", height: "2px", background: "rgba(255,255,255,0.07)" }} />
                  </div>
                  <div className="overlay" style={{ position: "absolute", inset: 0, background: "rgba(0,0,0,0.7)", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", opacity: 0, transition: "opacity 0.3s" }}>
                    <span style={{ color: "#a855f7", fontWeight: 800, fontSize: "0.8rem", letterSpacing: "0.1em", textTransform: "uppercase" }}>{item.cat}</span>
                    <span style={{ fontWeight: 700, marginTop: "0.5rem" }}>{item.label}</span>
                    <span style={{ color: "#c4b5fd", fontSize: "0.85rem", marginTop: "0.5rem" }}>Click to Preview</span>
                  </div>
                  <div style={{ position: "absolute", bottom: "1rem", left: "1rem" }}>
                    <span style={{ background: "rgba(168,85,247,0.3)", borderRadius: "20px", padding: "0.2rem 0.8rem", fontSize: "0.75rem", fontWeight: 600 }}>{item.cat}</span>
                  </div>
                </div>
              </FadeIn>
            ))}
          </div>
          <div style={{ textAlign: "center", marginTop: "3rem" }}>
            <button style={S.glowBtn()} onClick={() => navigate("Portfolio")} onMouseEnter={e => { e.target.style.boxShadow = "0 0 40px rgba(168,85,247,0.8)"; }} onMouseLeave={e => { e.target.style.boxShadow = "0 0 25px rgba(168,85,247,0.5)"; }}>
              View Full Portfolio
            </button>
          </div>
        </div>
      </section>

      {/* Testimonials */}
      <section style={{ padding: "6rem 2rem" }}>
        <div style={{ maxWidth: "800px", margin: "0 auto", textAlign: "center" }}>
          <FadeIn><div style={S.tag}>Testimonials</div></FadeIn>
          <FadeIn delay={0.1}><h2 style={{ ...S.heading, marginBottom: "3rem" }}>What Clients <span style={{ background: "linear-gradient(135deg,#a855f7,#7c3aed)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>Say</span></h2></FadeIn>
          <div style={{ ...S.glass, padding: "3rem", position: "relative", minHeight: "220px" }}>
            {TESTIMONIALS.map((t, i) => (
              <div key={i} style={{ position: i === 0 ? "relative" : "absolute", inset: 0, padding: "3rem", opacity: i === testimonialIdx ? 1 : 0, transition: "opacity 0.7s ease" }}>
                <div style={{ fontSize: "3rem", color: "#7c3aed", lineHeight: 1, marginBottom: "1rem" }}>"</div>
                <p style={{ color: "#e0d0ff", lineHeight: 1.8, fontSize: "1.05rem", fontStyle: "italic" }}>{t.text}</p>
                <div style={{ marginTop: "1.5rem" }}>
                  <div style={{ fontWeight: 800, color: "#a855f7" }}>{t.name}</div>
                  <div style={{ color: "#9ca3af", fontSize: "0.85rem" }}>{t.role}</div>
                </div>
              </div>
            ))}
          </div>
          <div style={{ display: "flex", justifyContent: "center", gap: "0.5rem", marginTop: "1.5rem" }}>
            {TESTIMONIALS.map((_, i) => (
              <button key={i} onClick={() => setTestimonialIdx(i)} style={{ width: i === testimonialIdx ? "24px" : "8px", height: "8px", borderRadius: "4px", background: i === testimonialIdx ? "#a855f7" : "rgba(168,85,247,0.3)", border: "none", cursor: "pointer", transition: "all 0.3s" }} />
            ))}
          </div>
        </div>
      </section>

      {/* Why Choose Us */}
      <section style={{ padding: "6rem 2rem", background: "rgba(124,58,237,0.04)" }}>
        <div style={{ maxWidth: "1200px", margin: "0 auto", display: "grid", gridTemplateColumns: "1fr 1fr", gap: "4rem", alignItems: "center" }}>
          <div>
            <FadeIn><div style={S.tag}>Why Us</div></FadeIn>
            <FadeIn delay={0.1}><h2 style={{ ...S.heading, marginBottom: "1rem" }}>Excellence in <span style={{ background: "linear-gradient(135deg,#a855f7,#7c3aed)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>Every Pixel</span></h2></FadeIn>
            <FadeIn delay={0.2}><p style={{ ...S.subheading, marginBottom: "2rem" }}>We combine creative vision with technical mastery to deliver designs that don't just look great — they perform.</p></FadeIn>
            <FadeIn delay={0.3}><button style={S.glowBtn()} onClick={() => navigate("Booking")} onMouseEnter={e => { e.target.style.boxShadow = "0 0 40px rgba(168,85,247,0.8)"; }} onMouseLeave={e => { e.target.style.boxShadow = "0 0 25px rgba(168,85,247,0.5)"; }}>Start Your Project</button></FadeIn>
          </div>
          <div style={{ display: "flex", flexDirection: "column", gap: "1rem" }}>
            {["Creative Excellence", "Premium Quality", "Fast Delivery", "Client Satisfaction", "Modern Design Approach"].map((item, i) => (
              <FadeIn key={i} delay={i * 0.1}>
                <div style={{ ...S.glass, padding: "1.2rem 1.5rem", display: "flex", alignItems: "center", gap: "1rem" }}>
                  <div style={{ width: "10px", height: "10px", borderRadius: "50%", background: "#a855f7", flexShrink: 0, boxShadow: "0 0 10px #a855f7" }} />
                  <span style={{ fontWeight: 600 }}>{item}</span>
                </div>
              </FadeIn>
            ))}
          </div>
        </div>
      </section>

      {/* Newsletter */}
      <section style={{ padding: "5rem 2rem" }}>
        <FadeIn>
          <div style={{ maxWidth: "600px", margin: "0 auto", textAlign: "center", ...S.glass, padding: "3rem" }}>
            <h2 style={{ ...S.heading, fontSize: "2rem", marginBottom: "0.5rem" }}>Stay in the Creative Loop</h2>
            <p style={{ color: "#9ca3af", marginBottom: "2rem" }}>Get design tips, portfolio updates, and exclusive offers.</p>
            <div style={{ display: "flex", gap: "0.75rem", flexWrap: "wrap" }}>
              <input placeholder="Your email address" style={{ ...S.input, flex: 1, minWidth: "200px" }} />
              <button style={{ ...S.glowBtn(true), whiteSpace: "nowrap" }} onClick={() => showToast("Subscribed! Welcome to the creative family 🎨")}>Subscribe</button>
            </div>
          </div>
        </FadeIn>
      </section>
    </div>
  );

  const AboutPage = () => (
    <div style={{ paddingTop: "70px" }}>
      {/* Hero */}
      <section style={{ padding: "6rem 2rem 3rem", textAlign: "center", position: "relative", overflow: "hidden" }}>
        <div style={{ position: "absolute", inset: 0, background: "radial-gradient(ellipse at center, rgba(124,58,237,0.2) 0%, transparent 65%)" }} />
        <div style={{ position: "relative", maxWidth: "800px", margin: "0 auto" }}>
          <FadeIn><div style={S.tag}>Our Story</div></FadeIn>
          <FadeIn delay={0.1}><h1 style={{ ...S.heading, fontSize: "clamp(2.5rem, 6vw, 4.5rem)", marginBottom: "1.5rem" }}>Creativity <span style={{ background: "linear-gradient(135deg,#a855f7,#7c3aed)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>Beyond Limits.</span></h1></FadeIn>
          <FadeIn delay={0.2}><p style={{ ...S.subheading, margin: "0 auto 2rem", textAlign: "center" }}>Danny Rich Visuals transforms ideas into visually powerful designs that help brands, businesses, events, and individuals stand out with confidence and style.</p></FadeIn>
          <FadeIn delay={0.3}>
            <div style={{ display: "flex", gap: "1rem", justifyContent: "center", flexWrap: "wrap" }}>
              <button style={S.glowBtn()} onClick={() => navigate("Portfolio")} onMouseEnter={e => { e.target.style.boxShadow = "0 0 40px rgba(168,85,247,0.8)"; }} onMouseLeave={e => { e.target.style.boxShadow = "0 0 25px rgba(168,85,247,0.5)"; }}>View Portfolio</button>
              <button style={S.outlineBtn()} onClick={() => navigate("Booking")} onMouseEnter={e => e.target.style.background = "rgba(168,85,247,0.1)"} onMouseLeave={e => e.target.style.background = "transparent"}>Book a Design</button>
            </div>
          </FadeIn>
        </div>
      </section>

      {/* About Content */}
      <section style={{ ...S.section }}>
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "4rem", alignItems: "center" }}>
          <div>
            <FadeIn><h2 style={{ ...S.heading, fontSize: "2rem", marginBottom: "1.5rem" }}>Who We Are</h2></FadeIn>
            {[
              "Danny Rich Visuals is a modern creative design brand focused on delivering high-quality visual experiences through professional graphic design and digital creativity.",
              "We specialize in creating eye-catching and impactful designs that communicate ideas clearly, attract attention, and elevate brands to a premium level.",
              "From business promotions and social media campaigns to church flyers, event graphics, and branding projects, every design is created with creativity, precision, and purpose.",
              "At Danny Rich Visuals, creativity is not just about making things look good — it is about creating visuals that connect, inspire, and leave lasting impressions.",
            ].map((txt, i) => (
              <FadeIn key={i} delay={i * 0.1}><p style={{ color: "#c4b5fd", lineHeight: 1.8, marginBottom: "1rem" }}>{txt}</p></FadeIn>
            ))}
          </div>
          {/* Founder visual placeholder */}
          <FadeIn delay={0.2}>
            <div style={{ ...S.glass, borderRadius: "24px", overflow: "hidden", aspectRatio: "4/5", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", position: "relative" }}>
              <div style={{ position: "absolute", inset: 0, background: "linear-gradient(135deg, rgba(124,58,237,0.2), rgba(76,29,149,0.1))" }} />
              <LogoSVG size={50} />
              <div style={{ marginTop: "2rem", textAlign: "center" }}>
                <div style={{ fontWeight: 800, fontSize: "1.2rem" }}>Danny Rich</div>
                <div style={{ color: "#a855f7", fontSize: "0.9rem" }}>Creative Director & Founder</div>
              </div>
            </div>
          </FadeIn>
        </div>
      </section>

      {/* Mission & Vision */}
      <section style={{ padding: "4rem 2rem", background: "rgba(124,58,237,0.05)" }}>
        <div style={{ maxWidth: "1200px", margin: "0 auto", display: "grid", gridTemplateColumns: "1fr 1fr", gap: "2rem" }}>
          {[
            { label: "Mission", icon: "◉", text: "To help brands, businesses, organizations, and individuals communicate their message through powerful and visually engaging designs." },
            { label: "Vision", icon: "◆", text: "To become a globally recognized creative brand known for innovation, excellence, and impactful visual storytelling." },
          ].map((item, i) => (
            <FadeIn key={i} delay={i * 0.15}>
              <div style={{ ...S.glass, padding: "2.5rem" }}>
                <div style={{ fontSize: "2rem", color: "#a855f7", marginBottom: "1rem" }}>{item.icon}</div>
                <h3 style={{ fontWeight: 800, fontSize: "1.3rem", marginBottom: "1rem", color: "#c4b5fd" }}>{item.label}</h3>
                <p style={{ color: "#9ca3af", lineHeight: 1.8, fontStyle: "italic" }}>"{item.text}"</p>
              </div>
            </FadeIn>
          ))}
        </div>
      </section>

      {/* Creative Process */}
      <section style={{ ...S.section }}>
        <FadeIn><div style={S.tag}>How We Work</div></FadeIn>
        <FadeIn delay={0.1}><h2 style={{ ...S.heading, marginBottom: "3rem" }}>Creative <span style={{ background: "linear-gradient(135deg,#a855f7,#7c3aed)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>Process</span></h2></FadeIn>
        <div style={{ display: "flex", flexDirection: "column", gap: "0" }}>
          {PROCESS.map((step, i) => (
            <FadeIn key={i} delay={i * 0.1}>
              <div style={{ display: "flex", gap: "2rem", alignItems: "flex-start", position: "relative", paddingBottom: "2rem" }}>
                <div style={{ flexShrink: 0, textAlign: "center" }}>
                  <div style={{ width: "60px", height: "60px", borderRadius: "50%", background: "linear-gradient(135deg,#7c3aed,#a855f7)", display: "flex", alignItems: "center", justifyContent: "center", fontWeight: 900, fontSize: "1rem", boxShadow: "0 0 20px rgba(168,85,247,0.4)" }}>{step.num}</div>
                  {i < PROCESS.length - 1 && <div style={{ width: "2px", height: "100%", background: "linear-gradient(to bottom, rgba(168,85,247,0.5), rgba(168,85,247,0.1))", margin: "0 auto", minHeight: "40px" }} />}
                </div>
                <div style={{ paddingTop: "0.8rem" }}>
                  <h3 style={{ fontWeight: 800, marginBottom: "0.5rem" }}>{step.title}</h3>
                  <p style={{ color: "#9ca3af", lineHeight: 1.7 }}>{step.desc}</p>
                </div>
              </div>
            </FadeIn>
          ))}
        </div>
      </section>

      {/* Stats */}
      <section ref={statsRef} style={{ background: "rgba(124,58,237,0.05)", borderTop: "1px solid rgba(168,85,247,0.1)", borderBottom: "1px solid rgba(168,85,247,0.1)", padding: "3rem 2rem" }}>
        <div style={{ maxWidth: "1200px", margin: "0 auto", display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(180px, 1fr))", gap: "1rem" }}>
          {STATS.map((s, i) => <StatCard key={i} {...s} active={statsInView} />)}
        </div>
      </section>
    </div>
  );

  const ServicesPage = () => (
    <div style={{ paddingTop: "70px" }}>
      <section style={{ padding: "6rem 2rem 3rem", textAlign: "center" }}>
        <div style={{ position: "absolute", inset: 0, background: "radial-gradient(ellipse at center top, rgba(124,58,237,0.15) 0%, transparent 60%)", pointerEvents: "none" }} />
        <FadeIn><div style={S.tag}>Our Services</div></FadeIn>
        <FadeIn delay={0.1}><h1 style={{ ...S.heading, fontSize: "clamp(2rem, 5vw, 4rem)", marginBottom: "1rem" }}>Design Solutions for <span style={{ background: "linear-gradient(135deg,#a855f7,#7c3aed)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>Every Vision</span></h1></FadeIn>
        <FadeIn delay={0.2}><p style={{ ...S.subheading, margin: "0 auto 4rem", textAlign: "center" }}>Premium design services crafted to elevate your brand to the next level.</p></FadeIn>
        <div style={{ maxWidth: "1200px", margin: "0 auto", display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(300px, 1fr))", gap: "1.5rem" }}>
          {SERVICES.map((sv, i) => (
            <FadeIn key={i} delay={i * 0.08}>
              <div style={{ ...S.glass, padding: "2.5rem", textAlign: "left", cursor: "pointer", transition: "all 0.3s", height: "100%" }}
                onMouseEnter={e => { e.currentTarget.style.borderColor = "rgba(168,85,247,0.6)"; e.currentTarget.style.boxShadow = "0 0 40px rgba(168,85,247,0.15)"; e.currentTarget.style.transform = "translateY(-8px)"; }}
                onMouseLeave={e => { e.currentTarget.style.borderColor = "rgba(168,85,247,0.2)"; e.currentTarget.style.boxShadow = "none"; e.currentTarget.style.transform = ""; }}>
                <div style={{ width: "60px", height: "60px", borderRadius: "16px", background: "linear-gradient(135deg, rgba(124,58,237,0.3), rgba(168,85,247,0.1))", display: "flex", alignItems: "center", justifyContent: "center", fontSize: "1.8rem", marginBottom: "1.5rem", color: "#a855f7" }}>{sv.icon}</div>
                <h3 style={{ fontWeight: 800, fontSize: "1.2rem", marginBottom: "0.75rem" }}>{sv.title}</h3>
                <p style={{ color: "#9ca3af", lineHeight: 1.7, fontSize: "0.95rem", marginBottom: "1.5rem" }}>{sv.desc}</p>
                <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginTop: "auto" }}>
                  <span style={{ color: "#c4b5fd", fontWeight: 700 }}>{sv.price}</span>
                  <button style={{ ...S.glowBtn(true) }} onClick={() => navigate("Booking")} onMouseEnter={e => { e.target.style.boxShadow = "0 0 30px rgba(168,85,247,0.7)"; }} onMouseLeave={e => { e.target.style.boxShadow = "0 0 15px rgba(168,85,247,0.4)"; }}>Order Now</button>
                </div>
              </div>
            </FadeIn>
          ))}
        </div>
      </section>
      <section style={{ padding: "4rem 2rem 6rem", textAlign: "center" }}>
        <FadeIn>
          <div style={{ maxWidth: "600px", margin: "0 auto", ...S.glass, padding: "3rem", borderColor: "rgba(168,85,247,0.4)" }}>
            <h2 style={{ fontWeight: 900, fontSize: "1.8rem", marginBottom: "1rem" }}>Need Something Custom?</h2>
            <p style={{ color: "#9ca3af", marginBottom: "2rem" }}>Have a unique project in mind? Let's talk and create something extraordinary together.</p>
            <button style={S.glowBtn()} onClick={() => navigate("Contact")} onMouseEnter={e => { e.target.style.boxShadow = "0 0 40px rgba(168,85,247,0.8)"; }} onMouseLeave={e => { e.target.style.boxShadow = "0 0 25px rgba(168,85,247,0.5)"; }}>Get Custom Quote</button>
          </div>
        </FadeIn>
      </section>
    </div>
  );

  const PortfolioPage = () => (
    <div style={{ paddingTop: "70px" }}>
      <section style={{ padding: "6rem 2rem" }}>
        <div style={{ maxWidth: "1200px", margin: "0 auto" }}>
          <FadeIn><div style={S.tag}>Portfolio</div></FadeIn>
          <FadeIn delay={0.1}><h1 style={{ ...S.heading, marginBottom: "3rem" }}>Our Creative <span style={{ background: "linear-gradient(135deg,#a855f7,#7c3aed)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>Showcase</span></h1></FadeIn>
          {/* Filter */}
          <div style={{ display: "flex", gap: "0.75rem", flexWrap: "wrap", marginBottom: "3rem" }}>
            {cats.map(cat => (
              <button key={cat} onClick={() => setPortfolioFilter(cat)} style={{
                background: portfolioFilter === cat ? "linear-gradient(135deg,#7c3aed,#a855f7)" : "rgba(168,85,247,0.08)",
                border: portfolioFilter === cat ? "none" : "1px solid rgba(168,85,247,0.25)",
                borderRadius: "50px", color: portfolioFilter === cat ? "white" : "#c4b5fd",
                padding: "0.5rem 1.2rem", cursor: "pointer", fontSize: "0.85rem", fontWeight: 600,
                transition: "all 0.3s", fontFamily: "inherit",
                boxShadow: portfolioFilter === cat ? "0 0 20px rgba(168,85,247,0.4)" : "none",
              }}>{cat}</button>
            ))}
          </div>
          {/* Masonry Grid */}
          <div style={{ columns: "3", columnGap: "1.5rem" }}>
            {filteredPortfolio.map((item, i) => (
              <div key={i} style={{ breakInside: "avoid", marginBottom: "1.5rem", borderRadius: "16px", overflow: "hidden", cursor: "pointer", position: "relative", height: `${item.h}px`, background: `linear-gradient(135deg, ${item.color}, #0d0020)`, transition: "transform 0.3s" }}
                onMouseEnter={e => { e.currentTarget.style.transform = "scale(1.02)"; e.currentTarget.querySelector(".port-overlay").style.opacity = "1"; }}
                onMouseLeave={e => { e.currentTarget.style.transform = ""; e.currentTarget.querySelector(".port-overlay").style.opacity = "0"; }}
                onClick={() => setModalItem(item)}>
                <div style={{ position: "absolute", inset: 0, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center" }}>
                  <div style={{ fontSize: "2.5rem", opacity: 0.4 }}>✦</div>
                </div>
                <div className="port-overlay" style={{ position: "absolute", inset: 0, background: "rgba(0,0,0,0.75)", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", opacity: 0, transition: "opacity 0.3s" }}>
                  <span style={{ color: "#a855f7", fontSize: "0.75rem", fontWeight: 700, letterSpacing: "0.1em", textTransform: "uppercase" }}>{item.cat}</span>
                  <span style={{ fontWeight: 800, marginTop: "0.5rem", textAlign: "center", padding: "0 1rem" }}>{item.label}</span>
                  <div style={{ marginTop: "1rem", width: "40px", height: "2px", background: "#a855f7" }} />
                </div>
                <div style={{ position: "absolute", bottom: "0.75rem", left: "0.75rem" }}>
                  <span style={{ background: "rgba(168,85,247,0.4)", borderRadius: "20px", padding: "0.2rem 0.7rem", fontSize: "0.7rem", fontWeight: 700 }}>{item.cat}</span>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Modal */}
      {modalItem && (
        <div style={{ position: "fixed", inset: 0, background: "rgba(0,0,0,0.92)", zIndex: 2000, display: "flex", alignItems: "center", justifyContent: "center", padding: "2rem" }} onClick={() => setModalItem(null)}>
          <div style={{ ...S.glass, maxWidth: "600px", width: "100%", padding: "3rem", borderColor: "rgba(168,85,247,0.5)" }} onClick={e => e.stopPropagation()}>
            <div style={{ height: "300px", borderRadius: "12px", background: `linear-gradient(135deg, ${modalItem.color}, #0d0020)`, display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "2rem" }}>
              <LogoSVG size={40} />
            </div>
            <span style={{ color: "#a855f7", fontSize: "0.8rem", fontWeight: 700, letterSpacing: "0.1em", textTransform: "uppercase" }}>{modalItem.cat}</span>
            <h2 style={{ fontWeight: 900, fontSize: "1.5rem", margin: "0.5rem 0 1rem" }}>{modalItem.label}</h2>
            <p style={{ color: "#9ca3af", lineHeight: 1.7, marginBottom: "2rem" }}>A premium creative design delivered with precision and artistic vision by Danny Rich Visuals.</p>
            <div style={{ display: "flex", gap: "1rem" }}>
              <button style={S.glowBtn(true)} onClick={() => { setModalItem(null); navigate("Booking"); }} onMouseEnter={e => { e.target.style.boxShadow = "0 0 30px rgba(168,85,247,0.7)"; }} onMouseLeave={e => { e.target.style.boxShadow = "0 0 15px rgba(168,85,247,0.4)"; }}>Order Similar</button>
              <button style={S.outlineBtn(true)} onClick={() => setModalItem(null)} onMouseEnter={e => e.target.style.background = "rgba(168,85,247,0.1)"} onMouseLeave={e => e.target.style.background = "transparent"}>Close</button>
            </div>
          </div>
        </div>
      )}
    </div>
  );

  const BookingPage = () => (
    <div style={{ paddingTop: "70px" }}>
      <section style={{ padding: "6rem 2rem" }}>
        <div style={{ maxWidth: "700px", margin: "0 auto" }}>
          <FadeIn><div style={S.tag}>Book a Design</div></FadeIn>
          <FadeIn delay={0.1}><h1 style={{ ...S.heading, marginBottom: "0.5rem" }}>Start Your <span style={{ background: "linear-gradient(135deg,#a855f7,#7c3aed)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>Project</span></h1></FadeIn>
          <FadeIn delay={0.2}><p style={{ ...S.subheading, marginBottom: "3rem" }}>Fill out the form below and we'll get back to you within 24 hours.</p></FadeIn>
          {/* Progress */}
          <FadeIn delay={0.2}>
            <div style={{ display: "flex", gap: "0.5rem", marginBottom: "3rem" }}>
              {[1, 2, 3].map(s => (
                <div key={s} style={{ flex: 1, height: "4px", borderRadius: "4px", background: s <= bookingStep ? "linear-gradient(90deg, #7c3aed, #a855f7)" : "rgba(168,85,247,0.2)", transition: "background 0.4s" }} />
              ))}
            </div>
            <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "2rem" }}>
              {["Project Details", "Your Info", "Confirm"].map((label, i) => (
                <span key={i} style={{ fontSize: "0.8rem", color: i + 1 <= bookingStep ? "#a855f7" : "#6b7280", fontWeight: i + 1 === bookingStep ? 700 : 500 }}>{label}</span>
              ))}
            </div>
          </FadeIn>
          <FadeIn delay={0.3}>
            <div style={{ ...S.glass, padding: "2.5rem" }}>
              {bookingStep === 1 && (
                <div style={{ display: "flex", flexDirection: "column", gap: "1.5rem" }}>
                  <h2 style={{ fontWeight: 800, marginBottom: "0.5rem" }}>Project Details</h2>
                  <div>
                    <label style={{ color: "#c4b5fd", fontSize: "0.85rem", marginBottom: "0.5rem", display: "block" }}>Service Needed *</label>
                    <select style={S.select} value={bookingData.service} onChange={e => setBookingData({ ...bookingData, service: e.target.value })}>
                      <option value="">Select a service...</option>
                      {SERVICES.map(sv => <option key={sv.title} value={sv.title}>{sv.title}</option>)}
                    </select>
                  </div>
                  <div>
                    <label style={{ color: "#c4b5fd", fontSize: "0.85rem", marginBottom: "0.5rem", display: "block" }}>Budget Range</label>
                    <select style={S.select} value={bookingData.budget} onChange={e => setBookingData({ ...bookingData, budget: e.target.value })}>
                      <option value="">Select budget...</option>
                      <option>Under $20</option><option>$20 - $50</option><option>$50 - $100</option><option>$100 - $200</option><option>$200+</option>
                    </select>
                  </div>
                  <div>
                    <label style={{ color: "#c4b5fd", fontSize: "0.85rem", marginBottom: "0.5rem", display: "block" }}>Deadline</label>
                    <input type="date" style={S.input} value={bookingData.deadline} onChange={e => setBookingData({ ...bookingData, deadline: e.target.value })} />
                  </div>
                  <div>
                    <label style={{ color: "#c4b5fd", fontSize: "0.85rem", marginBottom: "0.5rem", display: "block" }}>Project Details</label>
                    <textarea rows={4} placeholder="Describe your project, brand colors, style preferences..." style={{ ...S.input, resize: "vertical" }} value={bookingData.details} onChange={e => setBookingData({ ...bookingData, details: e.target.value })} />
                  </div>
                  <button style={S.glowBtn()} onClick={() => { if (!bookingData.service) { showToast("Please select a service."); return; } setBookingStep(2); }} onMouseEnter={e => { e.target.style.boxShadow = "0 0 40px rgba(168,85,247,0.8)"; }} onMouseLeave={e => { e.target.style.boxShadow = "0 0 25px rgba(168,85,247,0.5)"; }}>Continue →</button>
                </div>
              )}
              {bookingStep === 2 && (
                <div style={{ display: "flex", flexDirection: "column", gap: "1.5rem" }}>
                  <h2 style={{ fontWeight: 800, marginBottom: "0.5rem" }}>Your Information</h2>
                  <div>
                    <label style={{ color: "#c4b5fd", fontSize: "0.85rem", marginBottom: "0.5rem", display: "block" }}>Full Name *</label>
                    <input placeholder="Your full name" style={S.input} value={bookingData.name} onChange={e => setBookingData({ ...bookingData, name: e.target.value })} />
                  </div>
                  <div>
                    <label style={{ color: "#c4b5fd", fontSize: "0.85rem", marginBottom: "0.5rem", display: "block" }}>Email Address *</label>
                    <input type="email" placeholder="your@email.com" style={S.input} value={bookingData.email} onChange={e => setBookingData({ ...bookingData, email: e.target.value })} />
                  </div>
                  <div>
                    <label style={{ color: "#c4b5fd", fontSize: "0.85rem", marginBottom: "0.5rem", display: "block" }}>Phone / WhatsApp</label>
                    <input placeholder="+234..." style={S.input} value={bookingData.phone} onChange={e => setBookingData({ ...bookingData, phone: e.target.value })} />
                  </div>
                  <div style={{ display: "flex", gap: "1rem" }}>
                    <button style={S.outlineBtn()} onClick={() => setBookingStep(1)} onMouseEnter={e => e.target.style.background = "rgba(168,85,247,0.1)"} onMouseLeave={e => e.target.style.background = "transparent"}>← Back</button>
                    <button style={{ ...S.glowBtn(), flex: 1 }} onClick={() => { if (!bookingData.name || !bookingData.email) { showToast("Please fill in required fields."); return; } setBookingStep(3); }} onMouseEnter={e => { e.target.style.boxShadow = "0 0 40px rgba(168,85,247,0.8)"; }} onMouseLeave={e => { e.target.style.boxShadow = "0 0 25px rgba(168,85,247,0.5)"; }}>Continue →</button>
                  </div>
                </div>
              )}
              {bookingStep === 3 && (
                <div style={{ display: "flex", flexDirection: "column", gap: "1.5rem" }}>
                  <h2 style={{ fontWeight: 800, marginBottom: "0.5rem" }}>Confirm Your Request</h2>
                  {[["Service", bookingData.service], ["Budget", bookingData.budget], ["Deadline", bookingData.deadline], ["Name", bookingData.name], ["Email", bookingData.email], ["Phone", bookingData.phone]].filter(([, v]) => v).map(([k, v]) => (
                    <div key={k} style={{ display: "flex", justifyContent: "space-between", padding: "0.75rem 0", borderBottom: "1px solid rgba(168,85,247,0.1)" }}>
                      <span style={{ color: "#9ca3af", fontSize: "0.9rem" }}>{k}</span>
                      <span style={{ fontWeight: 600, fontSize: "0.9rem" }}>{v}</span>
                    </div>
                  ))}
                  <p style={{ color: "#9ca3af", fontSize: "0.85rem", lineHeight: 1.6 }}>By submitting, you agree to be contacted by Danny Rich Visuals regarding your design request. A deposit may be required before work begins.</p>
                  <div style={{ display: "flex", gap: "1rem" }}>
                    <button style={S.outlineBtn()} onClick={() => setBookingStep(2)} onMouseEnter={e => e.target.style.background = "rgba(168,85,247,0.1)"} onMouseLeave={e => e.target.style.background = "transparent"}>← Back</button>
                    <button style={{ ...S.glowBtn(), flex: 1 }} onClick={() => { showToast("🎨 Booking submitted! We'll contact you within 24 hours."); setBookingStep(1); setBookingData({ service: "", budget: "", deadline: "", name: "", email: "", phone: "", details: "" }); }} onMouseEnter={e => { e.target.style.boxShadow = "0 0 40px rgba(168,85,247,0.8)"; }} onMouseLeave={e => { e.target.style.boxShadow = "0 0 25px rgba(168,85,247,0.5)"; }}>Submit Request ✦</button>
                  </div>
                </div>
              )}
            </div>
          </FadeIn>
        </div>
      </section>
    </div>
  );

  const ContactPage = () => (
    <div style={{ paddingTop: "70px" }}>
      <section style={{ padding: "6rem 2rem" }}>
        <div style={{ maxWidth: "1200px", margin: "0 auto" }}>
          <FadeIn><div style={S.tag}>Get In Touch</div></FadeIn>
          <FadeIn delay={0.1}><h1 style={{ ...S.heading, marginBottom: "3rem" }}>Let's Create <span style={{ background: "linear-gradient(135deg,#a855f7,#7c3aed)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>Together</span></h1></FadeIn>
          <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "3rem" }}>
            {/* Form */}
            <FadeIn>
              <div style={{ ...S.glass, padding: "2.5rem", display: "flex", flexDirection: "column", gap: "1.5rem" }}>
                <h2 style={{ fontWeight: 800, fontSize: "1.3rem", marginBottom: "0.5rem" }}>Send a Message</h2>
                <input placeholder="Your Name" style={S.input} />
                <input type="email" placeholder="Email Address" style={S.input} />
                <input placeholder="Subject" style={S.input} />
                <textarea rows={5} placeholder="Your message..." style={{ ...S.input, resize: "vertical" }} />
                <button style={S.glowBtn()} onClick={() => showToast("Message sent! We'll reply within 24 hours. 🎨")} onMouseEnter={e => { e.target.style.boxShadow = "0 0 40px rgba(168,85,247,0.8)"; }} onMouseLeave={e => { e.target.style.boxShadow = "0 0 25px rgba(168,85,247,0.5)"; }}>Send Message ✦</button>
              </div>
            </FadeIn>
            {/* Contact Info */}
            <div style={{ display: "flex", flexDirection: "column", gap: "1.5rem" }}>
              <FadeIn delay={0.1}>
                <div style={{ ...S.glass, padding: "1.5rem", display: "flex", gap: "1rem", alignItems: "center" }}>
                  <div style={{ width: "50px", height: "50px", borderRadius: "12px", background: "linear-gradient(135deg, rgba(124,58,237,0.4), rgba(168,85,247,0.2))", display: "flex", alignItems: "center", justifyContent: "center", fontSize: "1.3rem", flexShrink: 0 }}>📞</div>
                  <div><div style={{ color: "#9ca3af", fontSize: "0.8rem", marginBottom: "0.25rem" }}>Phone / WhatsApp</div><div style={{ fontWeight: 700 }}>08144093156</div></div>
                </div>
              </FadeIn>
              <FadeIn delay={0.15}>
                <div style={{ ...S.glass, padding: "1.5rem", display: "flex", gap: "1rem", alignItems: "center" }}>
                  <div style={{ width: "50px", height: "50px", borderRadius: "12px", background: "linear-gradient(135deg, rgba(124,58,237,0.4), rgba(168,85,247,0.2))", display: "flex", alignItems: "center", justifyContent: "center", fontSize: "1.3rem", flexShrink: 0 }}>✉️</div>
                  <div><div style={{ color: "#9ca3af", fontSize: "0.8rem", marginBottom: "0.25rem" }}>Email</div><div style={{ fontWeight: 700 }}>philipdaniel820@gmail.com</div></div>
                </div>
              </FadeIn>
              <FadeIn delay={0.2}>
                <div style={{ ...S.glass, padding: "1.5rem" }}>
                  <div style={{ color: "#c4b5fd", fontSize: "0.85rem", marginBottom: "1rem", fontWeight: 700 }}>Follow on Social Media</div>
                  <div style={{ display: "flex", gap: "0.75rem", flexWrap: "wrap" }}>
                    {[{ label: "Facebook", handle: "Daniel Philips" }, { label: "Instagram", handle: "@Danny_rich" }, { label: "TikTok", handle: "@Danny_rich" }, { label: "X/Twitter", handle: "@Danny_rich" }].map((s, i) => (
                      <div key={i} style={{ ...S.glass, padding: "0.5rem 1rem", borderRadius: "50px", cursor: "pointer", transition: "all 0.3s" }}
                        onMouseEnter={e => { e.currentTarget.style.borderColor = "rgba(168,85,247,0.6)"; e.currentTarget.style.boxShadow = "0 0 15px rgba(168,85,247,0.2)"; }}
                        onMouseLeave={e => { e.currentTarget.style.borderColor = "rgba(168,85,247,0.2)"; e.currentTarget.style.boxShadow = "none"; }}>
                        <div style={{ fontSize: "0.7rem", color: "#a855f7", fontWeight: 700 }}>{s.label}</div>
                        <div style={{ fontSize: "0.8rem" }}>{s.handle}</div>
                      </div>
                    ))}
                  </div>
                </div>
              </FadeIn>
              {/* WhatsApp CTA */}
              <FadeIn delay={0.25}>
                <a href="https://wa.me/2348144093156" target="_blank" rel="noopener noreferrer" style={{ textDecoration: "none" }}>
                  <div style={{ background: "linear-gradient(135deg, #128C7E, #25D366)", borderRadius: "16px", padding: "1.5rem", display: "flex", alignItems: "center", gap: "1rem", cursor: "pointer", transition: "transform 0.3s", boxShadow: "0 0 25px rgba(37,211,102,0.3)" }}
                    onMouseEnter={e => { e.currentTarget.style.transform = "translateY(-3px)"; e.currentTarget.style.boxShadow = "0 0 40px rgba(37,211,102,0.5)"; }}
                    onMouseLeave={e => { e.currentTarget.style.transform = ""; e.currentTarget.style.boxShadow = "0 0 25px rgba(37,211,102,0.3)"; }}>
                    <span style={{ fontSize: "2rem" }}>💬</span>
                    <div>
                      <div style={{ fontWeight: 800, fontSize: "1rem" }}>Chat on WhatsApp</div>
                      <div style={{ fontSize: "0.85rem", opacity: 0.9 }}>Quick replies • 08144093156</div>
                    </div>
                  </div>
                </a>
              </FadeIn>
            </div>
          </div>

          {/* FAQ */}
          <div style={{ marginTop: "5rem" }}>
            <FadeIn><h2 style={{ ...S.heading, fontSize: "2rem", marginBottom: "2rem" }}>Frequently Asked <span style={{ background: "linear-gradient(135deg,#a855f7,#7c3aed)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>Questions</span></h2></FadeIn>
            <div style={{ display: "flex", flexDirection: "column", gap: "1rem" }}>
              {FAQ.map((faq, i) => (
                <FadeIn key={i} delay={i * 0.08}>
                  <div style={{ ...S.glass, borderRadius: "16px", overflow: "hidden", transition: "border-color 0.3s", borderColor: faqOpen === i ? "rgba(168,85,247,0.5)" : "rgba(168,85,247,0.2)" }}>
                    <button onClick={() => setFaqOpen(faqOpen === i ? null : i)} style={{ width: "100%", background: "none", border: "none", color: "white", padding: "1.3rem 1.5rem", display: "flex", justifyContent: "space-between", alignItems: "center", cursor: "pointer", fontWeight: 700, fontSize: "0.95rem", fontFamily: "inherit", textAlign: "left" }}>
                      {faq.q}
                      <span style={{ color: "#a855f7", transition: "transform 0.3s", transform: faqOpen === i ? "rotate(45deg)" : "rotate(0)", flexShrink: 0, marginLeft: "1rem", fontSize: "1.2rem" }}>+</span>
                    </button>
                    {faqOpen === i && (
                      <div style={{ padding: "0 1.5rem 1.3rem", color: "#9ca3af", lineHeight: 1.7, fontSize: "0.95rem" }}>{faq.a}</div>
                    )}
                  </div>
                </FadeIn>
              ))}
            </div>
          </div>
        </div>
      </section>
    </div>
  );

  const pages = { Home: <HomePage />, About: <AboutPage />, Services: <ServicesPage />, Portfolio: <PortfolioPage />, Booking: <BookingPage />, Contact: <ContactPage /> };

  return (
    <div style={S.root}>
      <style>{`
        * { margin: 0; padding: 0; box-sizing: border-box; }
        @keyframes pulse { 0%,100%{transform:scale(1);opacity:0.7} 50%{transform:scale(1.1);opacity:1} }
        @keyframes float0 { 0%,100%{transform:translateY(0) rotate(0deg)} 50%{transform:translateY(-20px) rotate(10deg)} }
        @keyframes float1 { 0%,100%{transform:translateY(0) rotate(0deg)} 50%{transform:translateY(15px) rotate(-8deg)} }
        @keyframes float2 { 0%,100%{transform:translateY(0) rotate(0deg)} 50%{transform:translateY(-12px) rotate(15deg)} }
        @keyframes bounce { 0%,100%{transform:translateX(-50%) translateY(0)} 50%{transform:translateX(-50%) translateY(-8px)} }
        @keyframes waSpin { from{transform:rotate(0)} to{transform:rotate(360deg)} }
        input::placeholder, textarea::placeholder { color: rgba(196,181,253,0.4); }
        ::-webkit-scrollbar { width:6px } ::-webkit-scrollbar-track{background:#050508} ::-webkit-scrollbar-thumb{background:#7c3aed;border-radius:3px}
        @media(max-width:768px) {
          .hide-mobile { display: none !important; }
          .mobile-cols { grid-template-columns: 1fr !important; }
          .masonry-mobile { columns: 1 !important; }
        }
      `}</style>

      {/* Navbar */}
      <nav style={S.nav}>
        <div style={{ cursor: "pointer" }} onClick={() => navigate("Home")}>
          <LogoSVG size={30} />
        </div>
        <div className="hide-mobile" style={{ display: "flex", alignItems: "center", gap: "0.25rem" }}>
          {NAV_LINKS.map(link => (
            <button key={link} style={S.navLink(page === link)} onClick={() => navigate(link)}
              onMouseEnter={e => { if (page !== link) e.target.style.color = "white"; }}
              onMouseLeave={e => { if (page !== link) e.target.style.color = "rgba(255,255,255,0.75)"; }}>
              {link}
            </button>
          ))}
          <button style={{ ...S.glowBtn(true), marginLeft: "1rem" }} onClick={() => navigate("Booking")} onMouseEnter={e => { e.target.style.boxShadow = "0 0 30px rgba(168,85,247,0.7)"; }} onMouseLeave={e => { e.target.style.boxShadow = "0 0 20px rgba(168,85,247,0.4)"; }}>Book Now</button>
        </div>
        {/* Mobile hamburger */}
        <button className="show-mobile" onClick={() => setMenuOpen(!menuOpen)} style={{ display: "none", background: "none", border: "none", color: "white", fontSize: "1.5rem", cursor: "pointer" }}>☰</button>
        <button style={{ display: "none", background: "none", border: "none", color: "white", fontSize: "1.5rem", cursor: "pointer" }} id="burger" onClick={() => setMenuOpen(!menuOpen)}>☰</button>
      </nav>
      {/* Simple mobile menu toggle via inline */}
      <div style={{ position: "fixed", top: "70px", left: 0, right: 0, background: "rgba(5,5,8,0.97)", backdropFilter: "blur(20px)", zIndex: 999, padding: menuOpen ? "1.5rem" : "0", maxHeight: menuOpen ? "500px" : "0", overflow: "hidden", transition: "all 0.4s ease", borderBottom: menuOpen ? "1px solid rgba(168,85,247,0.2)" : "none" }}>
        {NAV_LINKS.map(link => (
          <button key={link} style={{ display: "block", width: "100%", background: "none", border: "none", color: page === link ? "#a855f7" : "white", padding: "0.9rem 0", fontSize: "1rem", fontWeight: page === link ? 700 : 500, cursor: "pointer", textAlign: "left", borderBottom: "1px solid rgba(168,85,247,0.08)", fontFamily: "inherit", letterSpacing: "0.05em" }} onClick={() => navigate(link)}>
            {link}
          </button>
        ))}
        <button style={{ ...S.glowBtn(), marginTop: "1rem", width: "100%" }} onClick={() => navigate("Booking")}>Book Now</button>
      </div>

      {/* Page Content */}
      <main style={{ paddingTop: page === "Home" ? "0" : "0" }}>
        {pages[page]}
      </main>

      {/* Footer */}
      <footer style={{ background: "#020205", borderTop: "1px solid rgba(168,85,247,0.15)", padding: "4rem 2rem 2rem" }}>
        <div style={{ maxWidth: "1200px", margin: "0 auto" }}>
          <div style={{ display: "grid", gridTemplateColumns: "2fr 1fr 1fr 1fr", gap: "3rem", marginBottom: "3rem" }}>
            <div>
              <div style={{ marginBottom: "1.5rem", cursor: "pointer" }} onClick={() => navigate("Home")}><LogoSVG size={35} /></div>
              <p style={{ color: "#6b7280", lineHeight: 1.7, fontSize: "0.9rem", maxWidth: "280px" }}>Premium graphic design studio crafting visual experiences that elevate brands, businesses, and creatives worldwide.</p>
              <a href="https://wa.me/2348144093156" target="_blank" rel="noopener noreferrer" style={{ textDecoration: "none" }}>
                <div style={{ marginTop: "1.5rem", display: "inline-flex", alignItems: "center", gap: "0.5rem", background: "rgba(37,211,102,0.1)", border: "1px solid rgba(37,211,102,0.3)", borderRadius: "50px", padding: "0.5rem 1rem", cursor: "pointer" }}>
                  <span style={{ color: "#25D366", fontSize: "0.85rem" }}>💬</span><span style={{ color: "#25D366", fontSize: "0.85rem", fontWeight: 600 }}>WhatsApp Us</span>
                </div>
              </a>
            </div>
            <div>
              <h4 style={{ fontWeight: 800, marginBottom: "1.5rem", color: "#c4b5fd", fontSize: "0.9rem", letterSpacing: "0.1em", textTransform: "uppercase" }}>Pages</h4>
              {NAV_LINKS.map(link => <div key={link} onClick={() => navigate(link)} style={{ color: "#6b7280", marginBottom: "0.75rem", cursor: "pointer", fontSize: "0.9rem", transition: "color 0.3s" }} onMouseEnter={e => e.target.style.color = "#a855f7"} onMouseLeave={e => e.target.style.color = "#6b7280"}>{link}</div>)}
            </div>
            <div>
              <h4 style={{ fontWeight: 800, marginBottom: "1.5rem", color: "#c4b5fd", fontSize: "0.9rem", letterSpacing: "0.1em", textTransform: "uppercase" }}>Services</h4>
              {SERVICES.map(sv => <div key={sv.title} style={{ color: "#6b7280", marginBottom: "0.75rem", fontSize: "0.9rem" }}>{sv.title}</div>)}
            </div>
            <div>
              <h4 style={{ fontWeight: 800, marginBottom: "1.5rem", color: "#c4b5fd", fontSize: "0.9rem", letterSpacing: "0.1em", textTransform: "uppercase" }}>Contact</h4>
              <div style={{ color: "#6b7280", fontSize: "0.9rem", lineHeight: 2 }}>
                <div>📞 08144093156</div>
                <div>✉️ philipdaniel820@gmail.com</div>
                <div>📘 Daniel Philips</div>
                <div>📸 @Danny_rich</div>
                <div>🎵 @Danny_rich</div>
                <div>🐦 @Danny_rich</div>
              </div>
            </div>
          </div>
          <div style={{ borderTop: "1px solid rgba(168,85,247,0.1)", paddingTop: "2rem", display: "flex", justifyContent: "space-between", alignItems: "center", flexWrap: "wrap", gap: "1rem" }}>
            <span style={{ color: "#4b5563", fontSize: "0.85rem" }}>© 2025 Danny Rich Visuals. All rights reserved.</span>
            <span style={{ color: "#4b5563", fontSize: "0.85rem" }}>Crafted with ✦ and creativity</span>
          </div>
        </div>
      </footer>

      {/* Floating WhatsApp */}
      <a href="https://wa.me/2348144093156" target="_blank" rel="noopener noreferrer" style={{ position: "fixed", bottom: "2rem", right: "2rem", zIndex: 1500, textDecoration: "none" }}>
        <div style={{ width: "60px", height: "60px", borderRadius: "50%", background: "linear-gradient(135deg, #128C7E, #25D366)", display: "flex", alignItems: "center", justifyContent: "center", fontSize: "1.5rem", cursor: "pointer", boxShadow: "0 0 30px rgba(37,211,102,0.5)", transition: "all 0.3s" }}
          onMouseEnter={e => { e.currentTarget.style.transform = "scale(1.1)"; e.currentTarget.style.boxShadow = "0 0 50px rgba(37,211,102,0.7)"; }}
          onMouseLeave={e => { e.currentTarget.style.transform = ""; e.currentTarget.style.boxShadow = "0 0 30px rgba(37,211,102,0.5)"; }}>
          💬
        </div>
      </a>

      {/* Toast */}
      {toastMsg && (
        <div style={{ position: "fixed", bottom: "2rem", left: "50%", transform: "translateX(-50%)", background: "linear-gradient(135deg, #7c3aed, #a855f7)", borderRadius: "50px", padding: "1rem 2rem", color: "white", fontWeight: 700, zIndex: 3000, boxShadow: "0 0 30px rgba(168,85,247,0.6)", whiteSpace: "nowrap", animation: "fadeIn 0.3s ease" }}>
          {toastMsg}
        </div>
      )}
    </div>
  );
}
